# A+ Credit Restoration
Copy the `credit` folder into the root of the existing `atechspot` repository.
Add `_worker.js` at the repository root. If one already exists, merge the hostname routing instead of replacing it.
Commit and push, wait for Cloudflare Pages deployment, then test https://credit.atechspot.com.

Before accepting paying clients, have a qualified attorney review contracts, fees, cancellation notices, registration/bond requirements, and advertising.
